---
- name: Provisionning webservers group
  hosts: webservers
  vars:
    root_dir: '/opt'
    emil_password: password
    emil_password_salt: secret
    traefik_api_user: username
    traefik_api_password: password
    traefik_api_password_salt: secret
    acme_email: example@example.com
    acme_provider: namecheap # https://docs.traefik.io/configuration/acme/#provider
    namecheap_api_user: username
    namecheap_api_key: api_key
    docker_registry:
      - name: username
        password: password
        password_salt: secret
    domains:
      - name: example.com
        password: password
  roles:
    - docker
    - ddclient
    - mariadb
    - postgres
    - registry
    - watchtower
    - portainer
    - omnidb
    - nexus
    - bookstack
    - nextcloud
    - bitwarden
    - emil
    - minecraft
    - traefik
